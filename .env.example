# Solana Network (devnet, testnet, or mainnet-beta)
NEXT_PUBLIC_SOLANA_NETWORK=devnet

# Backend API URL (leave empty for Vercel deployment, use http://localhost:3001 for local development with separate backend)
NEXT_PUBLIC_BACKEND_URL=

# Pinata API Keys (required for image/metadata uploads on Vercel)
# PINATA_API_KEY=your_pinata_api_key
# PINATA_SECRET_KEY=your_pinata_secret_key
